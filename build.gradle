buildscript {
    repositories {
        google()

        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.3.2'
    }
}

allprojects {
    repositories {
        google()

        jcenter()

        maven { url 'https://github.com/Uncreated/UsedeskSDK/raw/master/' }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}